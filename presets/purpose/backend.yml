stacks:
  values:
    - app

artifacts:
  services:       { enabled: true }
  config_dir:     { enabled: true }
  docker_compose: { enabled: true }

validation:
  no_external_images:        { enabled: true }
  config_extends_valid:      { enabled: true }
  lifecycle_scripts_present: { enabled: true }
