# Fixture: the first rule hard-stops; the second must never be evaluated.
scripts:
  - id: always-fail
    name: "Always fail"
    command: |
      exit 3
rules:
  - type: script_reusable
    name: "Stops everything"
    script: always-fail
    severity: hard-stop
  - type: script_clean
    name: "Never reached"
    command: |
      true
